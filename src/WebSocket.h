#ifndef WEBSOCKET_H
#define WEBSOCKET_H

#include <iostream>

class WebSocket
{
    public:
        WebSocket(std::string url);
        virtual ~WebSocket();
    protected:
    private:
};

#endif // WEBSOCKET_H
